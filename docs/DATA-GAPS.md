# Data gaps and open items

Anything unverified, unreachable, or deferred. Every row must say what was tried and what to do next.
Sessions add rows; sessions that resolve one delete it and note the resolution in `docs/DECISIONS.md`.

## Open

| #   | Item                                                                             | Area       | What was tried                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Next step                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| --- | -------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `LICENSE` carries the literal placeholder `<YOUR NAME>`                          | Legal      | Kept the placeholder at the human's explicit instruction; `git config user.name` reads "Ayush Singh"                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Human replaces `<YOUR NAME>` with the copyright holder before the repo is made public                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 4   | "No user-entered value leaves the page" is still only checked on synthetic input | Privacy    | `tests/no-external-urls.test.ts` proves it statically; `tests/e2e/zero-third-party-requests.spec.ts` proves the runtime half over every route, a language toggle, and (Session 3A) reading the AI notice, accepting it and choosing a tier. `tests/e2e/ai-byok.spec.ts` covers the API key. **Session 4 closes the synthetic-input part**: the Law Converter's search box and offence-date picker are the first fields that capture what a reader typed, and the privacy spec now types a real query and a real offence date, opens a section, copies the citation, shares it and saves it, asserting nothing crosses the origin | Extend the same test again when Drafting Studio or the Pay calculator adds fields, and when an AI surface first sends a reader-typed question                                                                                                                                                                                                                                                                                                                                                                                 |
| 6   | The three law datasets are ingested; nothing else is                             | Data       | `scripts/ingest/ncrb_sankalan.py` produces `data/law/{bns,bnss,bsa}.json` and `data/law/index.json` from NCRB Sankalan, validated against `schemas/law-*.schema.json` at write time and `src/modules/law/schema.ts` in `pnpm test`. 1,059 sections, 0 missing                                                                                                                                                                                                                                                                                                                                                                    | Sessions 4+: pay matrix and DA/HRA/TA tables, the holiday O.M., the CSMOP document templates, the Rules Trainer question bank                                                                                                                                                                                                                                                                                                                                                                                                 |
| 7   | Every dark-mode reader sees one frame of light on each load                      | A11y/UX    | ADR-010 removes the `prefers-color-scheme` remap that ADR-006 added, so light is now what paints before hydration for everyone. A reader who chose dark sees one frame of light on every load, because IndexedDB cannot be read before first paint. This is wider than the ADR-006 version of the same gap, and it is the direct consequence of "dark only from the explicit toggle"                                                                                                                                                                                                                                             | Accept, or revisit only if readers report it — a synchronous pre-paint read would mean storing the preference outside IndexedDB, against a hard rule                                                                                                                                                                                                                                                                                                                                                                          |
| 8   | `@lhci/cli` can no longer produce a Lighthouse "PWA" category score              | Tooling    | `pnpm dlx @lhci/cli` resolves `lighthouse@12.6.1`; Lighthouse removed the PWA category and its audits (`installable-manifest`, `service-worker`, `maskable-icon`, …) upstream around v10. Verified installability instead with a one-off `pnpm dlx lighthouse@9` run against `pnpm preview` — see ADR-008                                                                                                                                                                                                                                                                                                                        | If a current-Lighthouse PWA signal is needed again, re-run the `lighthouse@9` one-off, or switch to Chrome DevTools Protocol's `Page.getInstallabilityErrors` directly                                                                                                                                                                                                                                                                                                                                                        |
| 9   | `OfflineBadge` can't tell a captive portal / dead uplink from a real connection  | PWA        | It reads `navigator.onLine`, which is true whenever there's a network interface — not whether the internet is actually reachable. Now that a service worker exists, a better signal is available                                                                                                                                                                                                                                                                                                                                                                                                                                 | Have the badge (or the SW) probe a small same-origin resource periodically or on `online`, rather than trusting `navigator.onLine` alone                                                                                                                                                                                                                                                                                                                                                                                      |
| 10  | Manifest `lang: 'hi'` sits in tension with "equal bilingual footing"             | Product    | This session's brief specified `lang: 'hi'` literally for the web manifest (`vite.config.ts`); the actual in-page language is detected per reader and is often English (`detectBrowserLanguage`)                                                                                                                                                                                                                                                                                                                                                                                                                                 | Human product decision: keep `hi`, switch to `en`, or accept the mismatch as a manifest-spec limitation (no per-reader manifest is practical) — see ADR-008 addendum                                                                                                                                                                                                                                                                                                                                                          |
| 11  | Devanagari headings sit at line-height 1.75, noticeably airier than Latin ones   | Typography | The master context says "Noto Sans Devanagari with line-height ≥ 1.75 always", stated twice, so headings are not exempt from the `:lang(hi)` floor in `src/styles/index.css`. Neev itself set Devanagari headings to 1.45 and only body copy to 1.75. Followed as written rather than silently narrowed                                                                                                                                                                                                                                                                                                                          | Human looks at a Hindi page on a device and says whether headings should get their own tighter floor (~1.45). One selector in `index.css` either way                                                                                                                                                                                                                                                                                                                                                                          |
| 12  | 320 KiB of `.woff` legacy fallbacks are deployed but never fetched               | Build      | `@fontsource` declares a `.woff` beside every `.woff2`, and Vite emits both because the package CSS references them. They are never requested (every target browser takes the woff2) and never precached (`globPatterns` names only `woff2`), so they cost no user bandwidth — only CDN storage                                                                                                                                                                                                                                                                                                                                  | Only worth fixing by hand-writing the `@font-face` rules against relative `node_modules` paths, which trades 320 KiB of idle CDN storage for a brittle path. Left alone deliberately                                                                                                                                                                                                                                                                                                                                          |
| 13  | Tier 0 (on-device) is an interface and a stub, not a working provider            | AI         | `LocalProvider` implements `AiProvider`, declares `localOnly: true` / `tools: false`, and throws `not_installed`. The master context puts @mlc-ai/web-llm 0.2.84 in Session 24; it needs a multi-hundred-megabyte weights download and a WebGPU adapter, both of which are the reader's explicit choice rather than something this build should smuggle in                                                                                                                                                                                                                                                                       | Session 24: implement over web-llm behind its own download consent, and decide whether a small local model can do tool calling reliably enough to set `capabilities.tools`                                                                                                                                                                                                                                                                                                                                                    |
| 14  | Tier 2 (hosted proxy) is complete in the client but has no Worker to talk to     | AI         | `ProxyProvider` speaks the same wire format as Tier 1 with no credential of any kind, and is unit-tested against an intercepted fetch. This build sets no `VITE_AI_PROXY_URL`, so `tierReady()` is false and the option is shown disabled with a reason                                                                                                                                                                                                                                                                                                                                                                          | Session 24: the Cloudflare Worker, plus its own privacy notice — it sees every prompt, and that is the operator's statement to make, not this repo's                                                                                                                                                                                                                                                                                                                                                                          |
| 15  | The AI answer cache's 0.92 similarity gate is a judgement, not a measurement     | AI         | Fuse over the normalised question with `ignoreLocation`, gated at similarity ≥ 0.92, chosen tight on purpose — this is a "you asked the same thing again" cache, not a "close enough" one. Unit-tested for exact matches, punctuation-only differences and clear non-matches, in both scripts                                                                                                                                                                                                                                                                                                                                    | Once real agents run, sample actual near-miss pairs and check the threshold against them rather than against invented examples                                                                                                                                                                                                                                                                                                                                                                                                |
| 16  | No official Hindi text of the three Sanhitas has been located                    | Data/i18n  | NCRB Sankalan publishes the tables, the section text and the First Schedule in English only — `GazetteBNS2023.pdf` and `BNS2023.pdf` contain no Devanagari at all (checked with PyMuPDF). India Code is unreachable (row 17). `legislative.gov.in` returns 404 for `A2023-45.pdf`; MHA hosts only `..._english_...` PDFs. Per the brief, nothing is machine-translated: 965 of 1,059 headings and all 1,059 section texts carry `hi: ""`, counted in the generated block below                                                                                                                                                   | Session 20: locate the Hindi gazette (Part II Section 1, Hindi edition) or the Legislative Department's Hindi acts listing, then feed it in as an overlay with `provenance.official: true`, replacing the curated Hindi                                                                                                                                                                                                                                                                                                       |
| 17  | India Code is unreachable, so no authoritative section text has been seeded      | Data       | `indiacode.gov.in` was rebuilt as an Angular application: its HTML response is a mount point with no server-side content. The legacy `www.indiacode.nic.in` DSpace host now serves only a migration notice, and its `/handle` and `/browse` endpoints 404. `scripts/ingest/indiacode_seed.py --discover` was run and recorded 0/248 sections — `scripts/ingest/reports/indiacode-coverage.json`. English section text was taken from NCRB Sankalan instead, which is the better public alternative and covers all 1,059 sections                                                                                                 | Session 20: browse India Code by hand once it is back, paste the section URLs into `scripts/ingest/indiacode_urls.json`, and run the seed. It stays off the cron either way (ADR-012)                                                                                                                                                                                                                                                                                                                                         |
| 18  | Hindi for 94 BNS sections is curated, not statutory                              | Data/i18n  | `data/law/overlays/bns-hindi-curated.json` supplies hand-authored Hindi headings and punishment for the most-cited BNS sections, because the alternative was an empty Hindi column on the sections officers use daily. Every one carries `verify: true` and a `provenance.official: false` block, and `tests/law-data.test.ts` fails if a curated section is not flagged                                                                                                                                                                                                                                                         | **Done in Session 4**: a `verify: true` section renders a marigold "Verify" banner on its card saying the Hindi is a Sahayak translation and not the statutory text, and `get_section` returns `hindiIsCurated` so an agent is told the same thing. Replace with the official Hindi once row 16 is closed                                                                                                                                                                                                                     |
| 19  | NCRB publishes no section text for BNSS 73 and BNSS 112                          | Data       | `ChaptersBNSS.html` carries the heading for both and nothing else — the provision text is simply absent from the portal, not lost by the parser (verified against the archived raw HTML). Everything else across all three codes has full English text                                                                                                                                                                                                                                                                                                                                                                           | Session 20: fill from the BNSS bare act (`DownloadPDF/BNSS2023.pdf`, pages parsed with `pdfplumber`) as a two-section overlay, or from India Code when row 17 clears                                                                                                                                                                                                                                                                                                                                                          |
| 20  | The `cytrain.ncrb.gov.in` mirror does not carry the Sankalan documents           | Data       | Every `SectionTable*.html` path on that host answered 404. It is kept as the second entry in `urls_for()` so a future run picks it up automatically if NCRB ever publishes there, and so the failure is recorded rather than rediscovered                                                                                                                                                                                                                                                                                                                                                                                        | Nothing to do. Revisit only if `www.ncrb.gov.in` starts failing                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 22  | `/law` with a section open scores 67 on Lighthouse's default MOBILE preset       | Perf       | Measured against `pnpm preview` after this session's work: `/law?q=302` scores **96 desktop** (FCP 0.6 s, LCP 1.4 s, TBT 70 ms, CLS 0) and **67 mobile** (FCP 2.6 s, LCP 4.8 s, TBT 470 ms). `/law` with no query scores 99 / 84 — loading the tables only once there is a query was worth 62 → 84 on mobile. The ceiling is the application shell, not the data: `/settings`, which loads no module data at all, scores **90** mobile. Under the mobile preset's simulated 1.6 Mbps link the 490 KB (gzip) of statute is ~2.5 s of transfer, and parsing 3.9 MB on a 4x-throttled CPU is most of the 470 ms TBT                 | Two levers, in order of value. (a) Split the section TEXT out of the searchable payload: measured, that is 391 KB gzip → **139 KB**, and would cut the parse by two thirds. It needs a build-time transformation emitting two modules per code from the one committed JSON, plus a text panel that fills in after the card renders. (b) Trim the application shell (117 KB gzip of `index.js`), which is what actually caps the score. Neither is worth doing without re-measuring first — the numbers above are the baseline |

| 23   | Every historical Dearness Allowance order before October 2025 is cited by number, not read | Data/Pay | The Department of Expenditure serves the current Dearness Allowance order at `circulars_document/DAorder7cpc.pdf` and the previous one at `DAeng7CPC.pdf`, replacing both on each revision, and archives none. Those two were fetched and OCR-read in full: 60% from 01.01.2026 (O.M. 1/1(i)/2026-E.II(B) of 22.04.2026) and 58% from 01.07.2025 (O.M. 1/4(i)/2025-E.II(B) of 06.10.2025), and the 55% order of 02.04.2025 is cited by number inside the October one. The other 19 entries in `data/pay/da-history.json` carry the rate, the effective date and the O.M. number, all `verify: true`, sourced to the Department's 7th CPC orders listing. `doe.gov.in/orders-circulars/20` through `/41` were scraped and carry no superseded Dearness Allowance order | Fetch the superseded orders from a Government archive that keeps them — the PIB release for each revision names the O.M. number, and `web.archive.org` holds the Department's listing pages. Clearing 19 rows at once is worth one deliberate pass |
| 24   | The pay matrix is generated from the gazette rule, not transcribed from the gazette | Data/Pay | The only published pay matrix is the scanned Schedule to the CCS (Revised Pay) Rules, 2016; `doe.gov.in` hosts no text-layer copy and the `thc.nic.in` mirror that was fetched is a 20-page image scan. `scripts/ingest/pay_matrix.py` therefore derives all 540 cells from 19 entry pays and 19 cell counts by the Commission's own rule (previous cell × 1.03, rounded to the nearest 100), and `verify_against_gazette()` checks the result against the terminal cell of every level and 11 interior cells read off that scan by eye. Every check passes, and `tests/pay-data.test.ts` re-derives the rule independently. Level 13 ships as substituted by the 2017 Amendment Rules (₹1,23,100 over 20 cells), which DoE O.M. 4-6/2017-IC of 28.09.2017 confirms in words | Nothing outstanding — the matrix carries `verify: false` because the anchors were read from the gazette. Re-run `pay_matrix.py --check` if a text-layer copy ever appears, and widen `SPOT_CHECKS` from it |
| 25   | Twenty-two of thirty-two allowances carry the 7th CPC's recommended rate, not a rate read from the order that notified it | Data/Pay | The Department of Expenditure publishes only a handful of the 2017 allowance orders. Eight were fetched and OCR-read in full and carry `verify: false`: House Rent Allowance (2/5/2017-E.II(B)), Transport Allowance (21/5/2017-E.II(B)), Tough Location and Tribal Area (3/1/2017-E.II(B)), Hard Area (13/1/2017-E.II(B)), Island Special Duty (12/1/2017-E.II(B)), Special Duty for the North East and Ladakh (11/1/2017-E.II(B)), Additional HRA (28/1/2017-E.II(B)) and Split Duty (27/1/2017-E.II(B)). For the other 22 the source is the 7th CPC report itself — which does have a text layer and was read directly — so the figure is what the Commission recommended, and the notifying order was not located | Work through the 22 in `data/pay/allowances.json` with `verify: true`, fetching the notifying O.M. for each from the ministry that issued it — Ministry of Home Affairs for the Central Armed Police Force allowances, Department of Personnel and Training for Children Education Allowance, Ministry of Railways for Running Allowance. Flip `verify` to false only against a fetched order |
| 26   | The Central Bureau of Investigation allowance is recorded at 20 per cent, and the brief said 25 | Data/Pay | The 7th CPC records the pre-revision structure at para 8.17.119 — 25% of basic pay for CBI officers up to Superintendent of Police, 15% for Deputy Inspector General and above and for non-executive cadres — and recommends at para 8.17.121 a single rationalised rate of 20% of basic pay for the Intelligence Bureau, CBI, National Investigation Agency, Parliament Duty Group and non-operational SPG staff. `data/pay/allowances.json` therefore carries 20% with `verify: true` and a note saying both numbers and where each comes from, rather than the 25% the brief named. The Department of Expenditure order giving effect to the recommendation could not be found on `doe.gov.in` | Human or a later session confirms which rate the CBI actually pays today. If the notified rate turned out to be 25%, the recommendation was not accepted for the CBI and the record needs both the rate and that fact |
| 27   | Sixty-six of sixty-seven posts have an entry Level that was not read from a recruitment notice | Data/Pay | `ssc.gov.in` is a client-rendered Angular application: `/notice-board` and every API path tried return the same 80 KB shell, exactly like India Code (row 17). `mha.gov.in`, `upsc.gov.in`, `indianrailways.gov.in`, `kvsangathan.nic.in` and `isro.gov.in` were probed and serve no fetchable post-wise pay table either. Only IB ACIO Grade-II/Executive carries `verify: false`, confirmed at Pay Level 7 with Grade Pay 4600 against published sources and the master context. Every other post in `data/pay/jobs.json` carries `verify: true` and a source note saying the Level was not read from a notice. `tests/pay-data.test.ts` does check each post's Grade Pay against the Level it claims, so an internally inconsistent row cannot ship | Download one SSC Combined Graduate Level notice by hand — its post-wise Annexure settles about twenty of these at once — and one Railway Recruitment Board centralised notice for the six railway posts. Levels 9 and 10 both carry Grade Pay 5400, so the pair must be confirmed by Level, not by Grade Pay |
| 28   | The Central Government Health Scheme contribution rates were not read from the Ministry's order | Data/Pay | `cghs.gov.in` did not answer a request within 40 seconds, twice. The rates in `data/pay/cghs.json` — ₹250 for Levels 1-5, ₹450 for Level 6, ₹650 for Levels 7-11, ₹1,000 for Level 12 and above, in force from 01.02.2017 — match the master context and published summaries, and the record carries `verify: true` and says so | Fetch M.o.H.F.W. O.M. No. S-11011/11/2016-CGHS(P)/EHS of 09.01.2017 when `cghs.gov.in` responds, and record the ward entitlements from the same order |
| 29   | The income tax figures were not read from the Finance Act or from incometax.gov.in | Data/Pay | `incometax.gov.in` returns a 301 and serves no fetchable document; `incometaxindia.gov.in` returns a 302. The slabs, standard deduction and rebate in `data/pay/tax.json` are those introduced by the Finance Act, 2025 and continued unchanged for the 2026-27 tax year. Every provision carries both the Income-tax Act, 1961 section an officer knows and the Income-tax Act, 2025 section where it was confirmed — 80C becomes 123 and 80CCD becomes 124 — and all 21 records carry `verify: true` | Fetch the Finance Act, 2026 and the Income-tax Act, 2025 from `egazette.gov.in` or the Legislative Department, confirm that Budget 2026 left the slabs alone, and complete the 1961 → 2025 section mapping for the provisions where `section2025` is still null |
| 30   | The Unified Pension Scheme notification number is not recorded | Data/Pay | `financialservices.gov.in/beta/en/ups` returns a 404. The rules in `data/pay/ups.json` — employee 10%, Government 18.5% (10% matching plus about 8.5% to the pool corpus), 50% of the last twelve months' average basic pay after 25 years of qualifying service, proportionate between 10 and 25 years, a floor of ₹10,000 a month, 60% family payout, a lump sum of one tenth of (last basic pay + Dearness Allowance) per completed six months, and dearness relief on both payouts — were read from the Pension Fund Regulatory and Development Authority's own scheme page, which does not state the notification number | Fetch the Ministry of Finance notification of 24.01.2025 and the PFRDA (Operationalisation of the Unified Pension Scheme) Regulations, 2025, and record the number, the option window and the treatment of past retirees |
| 31   | The July 2026 Dearness Allowance entry is a projection with no order behind it | Data/Pay | `data/pay/da-history.json` carries one forward-looking row: 63 per cent from 01.07.2026, `status: "projected"`, `verify: true`, with a range of 62 to 64 and a note saying no order has been issued. It is derived by applying the published formula to the Labour Bureau's monthly AICPI-IW series for July 2025 to June 2026; the monthly figures reported by different secondary sources for the first half of 2026 do not agree to the decimal, which is why a range is recorded rather than a point. `tests/pay-data.test.ts` asserts there is exactly one projection, that it is the last row, and that it is flagged | Replace the row with the notified rate when the Department of Expenditure issues the 01.07.2026 order — usually in October. Until then the Pay calculator must render `status: "projected"` differently from a notified rate, and must never use it for arrears |
| 32   | The Railway Kilometreage Allowance per-100-kilometre rates are not carried | Data/Pay | `data/pay/allowances.json` records the part of Running Allowance a calculator can actually use — the pay element of 30 per cent of basic pay, which counts as pay for pension, Dearness Allowance and House Rent Allowance — plus the Allowance in Lieu of Kilometreage and Accident Allowance formulae, all from the 7th CPC report Chapter 8.11. The per-100-km rates differ by category (Loco Pilot Mail, Passenger, Goods, Shunter grades, Assistant Loco Pilot, Second Fireman) and by whether the duty is shunting, and are notified by the Railway Board rather than by the Department of Expenditure | Fetch the Railway Board's current Running Allowance rate circular and add the per-category rates as a slab list on `running-allowance-railways`. Until then a railway pay estimate is basic pay plus the 30 per cent pay element, and the card must say the kilometreage part is not included |

## Sources

What has been fetched, and what has not. Recorded so a later session does not rediscover it.

| Source                                             | URL                                                                 | Note                                                                                                                                                          |
| -------------------------------------------------- | ------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **NCRB Sankalan — section tables** ✅ ingested     | https://www.ncrb.gov.in/uploads/SankalanPortal/SectionTableBNS.html | Rows are inline HTML in `#example`/`#exampleold`; `ScheduleBNSS.html` is the First Schedule and `ChaptersBNSS.html` §359 the compounding tables. English only |
| India Code ❌ unreachable                          | https://indiacode.gov.in                                            | Client-rendered SPA; legacy DSpace host retired. Crawling forbidden regardless — see row 17 and ADR-012                                                       |
| **DoE — 7th CPC report** ✅ ingested                | https://doe.gov.in/files/cenetral-pay_document/7cpc_report_eng.pdf  | 899 pages with a full text layer — the only pay source fetched here that needed no OCR. Source for the Risk and Hardship Matrix, Dress Allowance, Children Education Allowance, Detachment, Deputation, Running Allowance, Night Duty, NPA and the Special Security Allowances |
| **DoE — allowance and DA orders** ✅ ingested (OCR) | https://doe.gov.in/files/circulars_document/                        | Scanned images with no text layer. Read by rendering each page with PyMuPDF and running the macOS Vision text recogniser over it; the figures were then checked against the rendered page by eye. Eight allowance orders plus the two current DA orders |
| **CCS (Revised Pay) Rules, 2016** ✅ ingested       | https://thc.nic.in/Central%20Governmental%20Rules/                  | A 20-page image scan; `doe.gov.in` hosts no text-layer copy. Pages 12-15 are the pay matrix and were read visually. See `docs/DATA-GAPS.md` #24 and `scripts/ingest/README.md` |
| DoPT / DoPPW orders                                | various                                                             | PDF; needs `pdfplumber` / `PyMuPDF` in the CI pipeline                                                                                                        |
| ssc.gov.in ❌ unreachable                          | https://ssc.gov.in                                                  | Client-rendered Angular application, like India Code. `/notice-board` and every API path tried return the same 80 KB shell — see row 27                       |
| cghs.gov.in ❌ unreachable                         | https://cghs.gov.in                                                 | No response within 40 seconds, twice — see row 28                                                                                                             |
| incometax.gov.in ❌ unreachable                    | https://www.incometax.gov.in                                        | 301 with no body; `incometaxindia.gov.in` returns 302 — see row 29                                                                                            |
| **PFRDA — Unified Pension Scheme** ✅ ingested      | https://www.pfrda.org.in/schemes/national-pension-system/unified-pension-scheme | The regulator's own scheme page, read directly. It carries the rates and the payout rules but not the notification number — see row 30            |
| DoPT annual holiday O.M.                           | various                                                             | Published yearly; the calendar needs a refresh each December                                                                                                  |
| Dept of Official Language — Prashasanik Shabdavali | various                                                             | Source for the Hindi administrative glossary                                                                                                                  |
| data.gov.in                                        | https://data.gov.in                                                 | GODL-India licence — attribution is mandatory                                                                                                                 |

<!-- law-ingest:begin -->

### Law dataset coverage - generated by `scripts/ingest/ncrb_sankalan.py`

Counted on 2026-08-28 from the run that produced `data/law/*.json`. Do not edit by
hand: the ingest script rewrites everything between the two markers, and it formats this table the
way Prettier would so `pnpm format` and the weekly cron do not fight over it.

| Code          | Sections | Missing Hindi heading | Missing Hindi text | Missing English text | No classification | No Hindi punishment | No counterpart in the old Act |
| ------------- | -------: | --------------------: | -----------------: | -------------------: | ----------------: | ------------------: | ----------------------------: |
| BNS (`bns`)   |      358 |                   264 |                358 |                    0 |                70 |                 201 |                             0 |
| BNSS (`bnss`) |      531 |                   531 |                531 |                    2 |               n/a |                 n/a |                             0 |
| BSA (`bsa`)   |      170 |                   170 |                170 |                    0 |               n/a |                 n/a |                             0 |
| **All three** | **1059** |               **965** |           **1059** |                **2** |            **70** |             **201** |                         **0** |

The classification columns count only the BNS: cognizable, bailable and compoundable are properties
of offences, and the BNSS and the BSA are procedural.

Repealed-Act sections with no counterpart at all (NCRB marks them "Deleted"): **42** -
CrPC 2(f), CrPC 2(k), CrPC 2(q), CrPC 2(t), CrPC 8, CrPC 10, CrPC 16, CrPC 17, CrPC 18, CrPC 19, CrPC 27, CrPC 144A, CrPC 153, CrPC 355, CrPC 404, IEA 22A, IEA 65B(3)(a), IEA 65B(3)(b), IEA 65B(3)(c), IEA 65B(3)(d), IEA 82, IEA 88, IEA 113, IEA 166, ....

Parse path taken this run (see `scripts/ingest/README.md`): BNS `inline-html`, BNSS `inline-html`, BSA `inline-html`. `inline-html` means the
correspondence table's rows were in the HTML, as they have been on every run so far; `pdf` means they
were not, and `pdfplumber` read the bare-act PDF instead.

The full per-section lists are in `scripts/ingest/reports/law-gaps.json`, which is committed so a
reviewer can diff coverage between runs without re-running the ingest.

<!-- law-ingest:end -->
