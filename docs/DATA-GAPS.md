# Data gaps and open items

Anything unverified, unreachable, or deferred. Every row must say what was tried and what to do next.
Sessions add rows; sessions that resolve one delete it and note the resolution in `docs/DECISIONS.md`.

## Open

| #   | Item                                                                                                      | Area       | What was tried                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Next step                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| --- | --------------------------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `LICENSE` carries the literal placeholder `<YOUR NAME>`                                                   | Legal      | Kept the placeholder at the human's explicit instruction; `git config user.name` reads "Ayush Singh"                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Human replaces `<YOUR NAME>` with the copyright holder before the repo is made public                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 4   | "No user-entered value leaves the page" is still only checked on synthetic input                          | Privacy    | `tests/no-external-urls.test.ts` proves it statically; `tests/e2e/zero-third-party-requests.spec.ts` proves the runtime half over every route, a language toggle, and (Session 3A) reading the AI notice, accepting it and choosing a tier. `tests/e2e/ai-byok.spec.ts` covers the API key. **Session 4 closes the synthetic-input part**: the Law Converter's search box and offence-date picker are the first fields that capture what a reader typed, and the privacy spec now types a real query and a real offence date, opens a section, copies the citation, shares it and saves it, asserting nothing crosses the origin                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Session 8 extends it again**: `tests/e2e/draft.spec.ts` writes a whole Office Memorandum, exports a `.docx`, switches the preview language and reloads the restored draft with a request counter armed across the entire flow, asserting nothing crossed the origin. A draft is the most identifying thing this app can receive. Remaining: extend it again when an AI surface first sends a reader-typed question                                                                                                          |
| 6   | The three law datasets are ingested; nothing else is                                                      | Data       | `scripts/ingest/ncrb_sankalan.py` produces `data/law/{bns,bnss,bsa}.json` and `data/law/index.json` from NCRB Sankalan, validated against `schemas/law-*.schema.json` at write time and `src/modules/law/schema.ts` in `pnpm test`. 1,059 sections, 0 missing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Sessions 4+: pay matrix and DA/HRA/TA tables, the holiday O.M., the CSMOP document templates, the Rules Trainer question bank                                                                                                                                                                                                                                                                                                                                                                                                 |
| 7   | Every dark-mode reader sees one frame of light on each load                                               | A11y/UX    | ADR-010 removes the `prefers-color-scheme` remap that ADR-006 added, so light is now what paints before hydration for everyone. A reader who chose dark sees one frame of light on every load, because IndexedDB cannot be read before first paint. This is wider than the ADR-006 version of the same gap, and it is the direct consequence of "dark only from the explicit toggle"                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Accept, or revisit only if readers report it — a synchronous pre-paint read would mean storing the preference outside IndexedDB, against a hard rule                                                                                                                                                                                                                                                                                                                                                                          |
| 8   | `@lhci/cli` can no longer produce a Lighthouse "PWA" category score                                       | Tooling    | `pnpm dlx @lhci/cli` resolves `lighthouse@12.6.1`; Lighthouse removed the PWA category and its audits (`installable-manifest`, `service-worker`, `maskable-icon`, …) upstream around v10. Verified installability instead with a one-off `pnpm dlx lighthouse@9` run against `pnpm preview` — see ADR-008                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | If a current-Lighthouse PWA signal is needed again, re-run the `lighthouse@9` one-off, or switch to Chrome DevTools Protocol's `Page.getInstallabilityErrors` directly                                                                                                                                                                                                                                                                                                                                                        |
| 9   | `OfflineBadge` can't tell a captive portal / dead uplink from a real connection                           | PWA        | It reads `navigator.onLine`, which is true whenever there's a network interface — not whether the internet is actually reachable. Now that a service worker exists, a better signal is available                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Have the badge (or the SW) probe a small same-origin resource periodically or on `online`, rather than trusting `navigator.onLine` alone                                                                                                                                                                                                                                                                                                                                                                                      |
| 10  | Manifest `lang: 'hi'` sits in tension with "equal bilingual footing"                                      | Product    | This session's brief specified `lang: 'hi'` literally for the web manifest (`vite.config.ts`); the actual in-page language is detected per reader and is often English (`detectBrowserLanguage`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Human product decision: keep `hi`, switch to `en`, or accept the mismatch as a manifest-spec limitation (no per-reader manifest is practical) — see ADR-008 addendum                                                                                                                                                                                                                                                                                                                                                          |
| 11  | Devanagari headings sit at line-height 1.75, noticeably airier than Latin ones                            | Typography | The master context says "Noto Sans Devanagari with line-height ≥ 1.75 always", stated twice, so headings are not exempt from the `:lang(hi)` floor in `src/styles/index.css`. Neev itself set Devanagari headings to 1.45 and only body copy to 1.75. Followed as written rather than silently narrowed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Human looks at a Hindi page on a device and says whether headings should get their own tighter floor (~1.45). One selector in `index.css` either way                                                                                                                                                                                                                                                                                                                                                                          |
| 12  | 320 KiB of `.woff` legacy fallbacks are deployed but never fetched                                        | Build      | `@fontsource` declares a `.woff` beside every `.woff2`, and Vite emits both because the package CSS references them. They are never requested (every target browser takes the woff2) and never precached (`globPatterns` names only `woff2`), so they cost no user bandwidth — only CDN storage                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Only worth fixing by hand-writing the `@font-face` rules against relative `node_modules` paths, which trades 320 KiB of idle CDN storage for a brittle path. Left alone deliberately                                                                                                                                                                                                                                                                                                                                          |
| 13  | Tier 0's on-device answers are markedly weaker than the paid tiers, and no amount of prompting fixes that | AI         | **The provider itself is no longer a gap — Session 24 shipped it** (ADR-037): `src/ai/providers/local.ts` runs `@mlc-ai/web-llm` 0.2.84 over five curated models, with an emulated tool protocol, a WebGPU probe, a memory guard, a resumable Cache-API download and an unload control. What remains is the honest limit of a 0.9–2.5 GB model: it writes rough Hindi (the picker says so per model — `ai.local.hindi.*`), it plans at most two lookups deep (`LOCAL_MAX_TOOL_STEPS`), and it will sometimes answer a question it should have looked up. That last one fails CLOSED — `runAgent`'s grounding rule and `validateCitations` discard an uncited answer — so the visible failure mode on Tier 0 is "that run did not finish" more often than on Tiers 1 and 2, which is the checks working rather than the tier being broken. Not measured against a real model on real questions this session: headless Chromium has no WebGPU adapter, so `tests/e2e/ai-local.spec.ts` proves where the download goes and that the section renders, not how good the answers are           | Run the four agents against a real download on a real machine and record, per model, how often each finishes and how usable the Hindi is — then either narrow the catalogue to the two that earn their download, or add a per-tier note to the surfaces that says which questions Tier 0 is worth asking. Needs a human at a laptop with a GPU; nothing in CI can produce this number                                                                                                                                         |
| 14  | The Tier 2 proxy exists and is deployed nowhere                                                           | AI/Ops     | **The Worker itself is no longer a gap — Session 24 wrote it** (`worker/`, ADR-037): four guards (origin, per-IP rate limit, model allowlist, daily token budget), SSE streamed through, no body logged, 42 tests of which 11 run the real Worker in miniflare, and a `workflow_dispatch`-only deploy workflow. What is open is that nobody has deployed it: this repository ships with no `VITE_AI_PROXY_URL`, so the tier's row is not drawn, and the Worker has never run against a real Anthropic key or real traffic. Two things are therefore unmeasured — whether KV's one-write-per-second-per-key limit undercounts the daily budget under real concurrency (the code says it can and `worker/README.md` says so out loud), and whether the streamed pass-through holds up over a slow mobile link                                                                                                                                                                                                                                                                              | Deploy it when credits are available, following `docs/AI.md` §12's order (deploy → set `VITE_AI_PROXY_URL` → rebuild). Then watch the budget key for a week against the Cloudflare request count; if they disagree by more than a few per cent, move the counter to a Durable Object, which `worker/README.md` names as the upgrade path                                                                                                                                                                                      |
| 15  | The AI answer cache's 0.92 similarity gate is a judgement, not a measurement                              | AI         | Fuse over the normalised question with `ignoreLocation`, gated at similarity ≥ 0.92, chosen tight on purpose — this is a "you asked the same thing again" cache, not a "close enough" one. Unit-tested for exact matches, punctuation-only differences and clear non-matches, in both scripts                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Once real agents run, sample actual near-miss pairs and check the threshold against them rather than against invented examples                                                                                                                                                                                                                                                                                                                                                                                                |
| 16  | No official Hindi text of the three Sanhitas has been located                                             | Data/i18n  | Session 20 re-attempted every avenue: `indiacode.nic.in/handle/...` (403), `egazette.gov.in`’s own PDF (certificate verification failure — the same class of failure `authoring_common.build_ca_bundle()` already works around in the Python pipeline, but that fix is not available to `WebFetch`), and `mha.gov.in`’s twin `..._english_.../..._hindi_...` filename convention (the Hindi variant also 403s). NCRB Sankalan itself remains English-only (`GazetteBNS2023.pdf`, `BNS2023.pdf`, checked with PyMuPDF). The gap is now confirmed rather than merely unattempted a second time. All 1,059 section **texts** still carry `hi: ""` — nothing here is machine-translated, per the brief — but the **headings** are no longer empty: see row 18, extended this session to all 1,059 headings across BNS/BNSS/BSA via the curated-overlay route, since heading translation is a bounded, reviewable unit of work that full statutory text is not                                                                                                                                | If the official Hindi Sanhitas are ever located, they settle two things at once: they replace the curated headings in row 18 with `provenance.official: true`, and they are the only route to the still-open section-TEXT gap, which curation was deliberately not extended to (translating ~1,059 full statutory sections is a different order of risk than translating a heading)                                                                                                                                           |
| 17  | India Code is unreachable, so no authoritative section text has been seeded                               | Data       | `indiacode.gov.in` was rebuilt as an Angular application: its HTML response is a mount point with no server-side content. The legacy `www.indiacode.nic.in` DSpace host now serves only a migration notice, and its `/handle` and `/browse` endpoints 404. `scripts/ingest/indiacode_seed.py --discover` was run and recorded 0/248 sections — `scripts/ingest/reports/indiacode-coverage.json`. English section text was taken from NCRB Sankalan instead, which is the better public alternative and covers all 1,059 sections                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Session 20: browse India Code by hand once it is back, paste the section URLs into `scripts/ingest/indiacode_urls.json`, and run the seed. It stays off the cron either way (ADR-012)                                                                                                                                                                                                                                                                                                                                         |
| 18  | Hindi for every BNS/BNSS/BSA section heading is curated, not statutory                                    | Data/i18n  | `data/law/overlays/{bns,bnss,bsa}-hindi-curated.json` supply hand-authored Hindi headings for all 1,059 sections (extended this session from 94 BNS sections to full coverage of all three codes, plus Hindi punishment text for the 201 classified BNS sections that had a curated heading but no punishment translation), because the alternative was an empty Hindi column on every section an officer might look up (row 16). Every one carries `verify: true` and a `provenance.official: false` block, and `tests/law-data.test.ts` fails if a curated section is not flagged, or if a classified-and-curated section has no Hindi punishment                                                                                                                                                                                                                                                                                                                                                                                                                                      | A `verify: true` section renders a marigold "Verify" banner on its card saying the Hindi is a Sahayak translation and not the statutory text, and `get_section` returns `hindiIsCurated` so an agent is told the same thing. Replace with the official Hindi, section by section, once row 16 is closed                                                                                                                                                                                                                       |
| 19  | NCRB publishes no section text for BNSS 73 and BNSS 112                                                   | Data       | `ChaptersBNSS.html` carries the heading for both and nothing else — the provision text is simply absent from the portal, not lost by the parser (verified against the archived raw HTML). Everything else across all three codes has full English text                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Session 20: fill from the BNSS bare act (`DownloadPDF/BNSS2023.pdf`, pages parsed with `pdfplumber`) as a two-section overlay, or from India Code when row 17 clears                                                                                                                                                                                                                                                                                                                                                          |
| 20  | The `cytrain.ncrb.gov.in` mirror does not carry the Sankalan documents                                    | Data       | Every `SectionTable*.html` path on that host answered 404. It is kept as the second entry in `urls_for()` so a future run picks it up automatically if NCRB ever publishes there, and so the failure is recorded rather than rediscovered                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Nothing to do. Revisit only if `www.ncrb.gov.in` starts failing                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 22  | Every data-carrying route scores below 90 on Lighthouse's default MOBILE preset                           | Perf       | Re-measured against `pnpm preview` in Session 5. `/pay?job=ib-acio-ii-executive&city=delhi&da=60` scores **99 desktop** (FCP 0.6 s, LCP 0.9 s, TBT 0 ms, CLS 0) and **78 mobile** (FCP 2.9 s, LCP 4.6 s, TBT 110 ms); bare `/pay` scores 78 mobile too. Starting the dataset load at module scope rather than in an effect halved the gap between the route chunk arriving and the first dataset request (89 ms → 42 ms unthrottled) and did not move the score. From Session 4, unchanged: `/law?q=302` scores **96 desktop** (FCP 0.6 s, LCP 1.4 s, TBT 70 ms, CLS 0) and **67 mobile** (FCP 2.6 s, LCP 4.8 s, TBT 470 ms). `/law` with no query scores 99 / 84 — loading the tables only once there is a query was worth 62 → 84 on mobile. The ceiling is the application shell, not the data: `/settings`, which loads no module data at all, scores **89** mobile on the Session 5 run (90 in Session 4). Under the mobile preset's simulated 1.6 Mbps link the 490 KB (gzip) of statute is ~2.5 s of transfer, and parsing 3.9 MB on a 4x-throttled CPU is most of the 470 ms TBT | Two levers, in order of value. (a) Split the section TEXT out of the searchable payload: measured, that is 391 KB gzip → **139 KB**, and would cut the parse by two thirds. It needs a build-time transformation emitting two modules per code from the one committed JSON, plus a text panel that fills in after the card renders. (b) Trim the application shell (117 KB gzip of `index.js`), which is what actually caps the score. Neither is worth doing without re-measuring first — the numbers above are the baseline |

| 23 | Every historical Dearness Allowance order before October 2025 is cited by number, not read | Data/Pay | The Department of Expenditure serves the current Dearness Allowance order at `circulars_document/DAorder7cpc.pdf` and the previous one at `DAeng7CPC.pdf`, replacing both on each revision, and archives none. Those two were fetched and OCR-read in full: 60% from 01.01.2026 (O.M. 1/1(i)/2026-E.II(B) of 22.04.2026) and 58% from 01.07.2025 (O.M. 1/4(i)/2025-E.II(B) of 06.10.2025), and the 55% order of 02.04.2025 is cited by number inside the October one. The other 19 entries in `data/pay/da-history.json` carry the rate, the effective date and the O.M. number, all `verify: true`, sourced to the Department's 7th CPC orders listing. `doe.gov.in/orders-circulars/20` through `/41` were scraped and carry no superseded Dearness Allowance order | Fetch the superseded orders from a Government archive that keeps them — the PIB release for each revision names the O.M. number, and `web.archive.org` holds the Department's listing pages. Clearing 19 rows at once is worth one deliberate pass |
| 24 | The pay matrix is generated from the gazette rule, not transcribed from the gazette | Data/Pay | The only published pay matrix is the scanned Schedule to the CCS (Revised Pay) Rules, 2016; `doe.gov.in` hosts no text-layer copy and the `thc.nic.in` mirror that was fetched is a 20-page image scan. `scripts/ingest/pay_matrix.py` therefore derives all 540 cells from 19 entry pays and 19 cell counts by the Commission's own rule (previous cell × 1.03, rounded to the nearest 100), and `verify_against_gazette()` checks the result against the terminal cell of every level and 11 interior cells read off that scan by eye. Every check passes, and `tests/pay-data.test.ts` re-derives the rule independently. Level 13 ships as substituted by the 2017 Amendment Rules (₹1,23,100 over 20 cells), which DoE O.M. 4-6/2017-IC of 28.09.2017 confirms in words | Nothing outstanding — the matrix carries `verify: false` because the anchors were read from the gazette. Re-run `pay_matrix.py --check` if a text-layer copy ever appears, and widen `SPOT_CHECKS` from it |
| 25 | Twenty-two of thirty-two allowances carry the 7th CPC's recommended rate, not a rate read from the order that notified it | Data/Pay | The Department of Expenditure publishes only a handful of the 2017 allowance orders. Eight were fetched and OCR-read in full and carry `verify: false`: House Rent Allowance (2/5/2017-E.II(B)), Transport Allowance (21/5/2017-E.II(B)), Tough Location and Tribal Area (3/1/2017-E.II(B)), Hard Area (13/1/2017-E.II(B)), Island Special Duty (12/1/2017-E.II(B)), Special Duty for the North East and Ladakh (11/1/2017-E.II(B)), Additional HRA (28/1/2017-E.II(B)) and Split Duty (27/1/2017-E.II(B)). For the other 22 the source is the 7th CPC report itself — which does have a text layer and was read directly — so the figure is what the Commission recommended, and the notifying order was not located | Work through the 22 in `data/pay/allowances.json` with `verify: true`, fetching the notifying O.M. for each from the ministry that issued it — Ministry of Home Affairs for the Central Armed Police Force allowances, Department of Personnel and Training for Children Education Allowance, Ministry of Railways for Running Allowance. Flip `verify` to false only against a fetched order |
| 26 | The Central Bureau of Investigation allowance is recorded at 20 per cent, and the brief said 25 | Data/Pay | The 7th CPC records the pre-revision structure at para 8.17.119 — 25% of basic pay for CBI officers up to Superintendent of Police, 15% for Deputy Inspector General and above and for non-executive cadres — and recommends at para 8.17.121 a single rationalised rate of 20% of basic pay for the Intelligence Bureau, CBI, National Investigation Agency, Parliament Duty Group and non-operational SPG staff. `data/pay/allowances.json` therefore carries 20% with `verify: true` and a note saying both numbers and where each comes from, rather than the 25% the brief named. The Department of Expenditure order giving effect to the recommendation could not be found on `doe.gov.in` | Human or a later session confirms which rate the CBI actually pays today. If the notified rate turned out to be 25%, the recommendation was not accepted for the CBI and the record needs both the rate and that fact |
| 27 | Sixty-six of sixty-seven posts have an entry Level that was not read from a recruitment notice | Data/Pay | `ssc.gov.in` is a client-rendered Angular application: `/notice-board` and every API path tried return the same 80 KB shell, exactly like India Code (row 17). `mha.gov.in`, `upsc.gov.in`, `indianrailways.gov.in`, `kvsangathan.nic.in` and `isro.gov.in` were probed and serve no fetchable post-wise pay table either. Only IB ACIO Grade-II/Executive carries `verify: false`, confirmed at Pay Level 7 with Grade Pay 4600 against published sources and the master context. Every other post in `data/pay/jobs.json` carries `verify: true` and a source note saying the Level was not read from a notice. `tests/pay-data.test.ts` does check each post's Grade Pay against the Level it claims, so an internally inconsistent row cannot ship | Download one SSC Combined Graduate Level notice by hand — its post-wise Annexure settles about twenty of these at once — and one Railway Recruitment Board centralised notice for the six railway posts. Levels 9 and 10 both carry Grade Pay 5400, so the pair must be confirmed by Level, not by Grade Pay |
| 28 | The Central Government Health Scheme contribution rates were not read from the Ministry's order | Data/Pay | `cghs.gov.in` did not answer a request within 40 seconds, twice. The rates in `data/pay/cghs.json` — ₹250 for Levels 1-5, ₹450 for Level 6, ₹650 for Levels 7-11, ₹1,000 for Level 12 and above, in force from 01.02.2017 — match the master context and published summaries, and the record carries `verify: true` and says so | Fetch M.o.H.F.W. O.M. No. S-11011/11/2016-CGHS(P)/EHS of 09.01.2017 when `cghs.gov.in` responds, and record the ward entitlements from the same order |
| 29 | The income tax figures were not read from the Finance Act or from incometax.gov.in | Data/Pay | `incometax.gov.in` returns a 301 and serves no fetchable document; `incometaxindia.gov.in` returns a 302. The slabs, standard deduction and rebate in `data/pay/tax.json` are those introduced by the Finance Act, 2025 and continued unchanged for the 2026-27 tax year. Every provision carries both the Income-tax Act, 1961 section an officer knows and the Income-tax Act, 2025 section where it was confirmed — 80C becomes 123 and 80CCD becomes 124 — and all 21 records carry `verify: true` | Fetch the Finance Act, 2026 and the Income-tax Act, 2025 from `egazette.gov.in` or the Legislative Department, confirm that Budget 2026 left the slabs alone, and complete the 1961 → 2025 section mapping for the provisions where `section2025` is still null |
| 30 | The Unified Pension Scheme notification number is not recorded | Data/Pay | `financialservices.gov.in/beta/en/ups` returns a 404. The rules in `data/pay/ups.json` — employee 10%, Government 18.5% (10% matching plus about 8.5% to the pool corpus), 50% of the last twelve months' average basic pay after 25 years of qualifying service, proportionate between 10 and 25 years, a floor of ₹10,000 a month, 60% family payout, a lump sum of one tenth of (last basic pay + Dearness Allowance) per completed six months, and dearness relief on both payouts — were read from the Pension Fund Regulatory and Development Authority's own scheme page, which does not state the notification number | Fetch the Ministry of Finance notification of 24.01.2025 and the PFRDA (Operationalisation of the Unified Pension Scheme) Regulations, 2025, and record the number, the option window and the treatment of past retirees |
| 31 | The July 2026 Dearness Allowance entry is a projection with no order behind it | Data/Pay | `data/pay/da-history.json` carries one forward-looking row: 63 per cent from 01.07.2026, `status: "projected"`, `verify: true`, with a range of 62 to 64 and a note saying no order has been issued. It is derived by applying the published formula to the Labour Bureau's monthly AICPI-IW series for July 2025 to June 2026; the monthly figures reported by different secondary sources for the first half of 2026 do not agree to the decimal, which is why a range is recorded rather than a point. `tests/pay-data.test.ts` asserts there is exactly one projection, that it is the last row, and that it is flagged | Replace the row with the notified rate when the Department of Expenditure issues the 01.07.2026 order — usually in October. Until then the Pay calculator must render `status: "projected"` differently from a notified rate, and must never use it for arrears |
| 32 | The Railway Kilometreage Allowance per-100-kilometre rates are not carried | Data/Pay | `data/pay/allowances.json` records the part of Running Allowance a calculator can actually use — the pay element of 30 per cent of basic pay, which counts as pay for pension, Dearness Allowance and House Rent Allowance — plus the Allowance in Lieu of Kilometreage and Accident Allowance formulae, all from the 7th CPC report Chapter 8.11. The per-100-km rates differ by category (Loco Pilot Mail, Passenger, Goods, Shunter grades, Assistant Loco Pilot, Second Fireman) and by whether the duty is shunting, and are notified by the Railway Board rather than by the Department of Expenditure | Fetch the Railway Board's current Running Allowance rate circular and add the per-category rates as a slab list on `running-allowance-railways`. Until then a railway pay estimate is basic pay plus the 30 per cent pay element, and the card must say the kilometreage part is not included |

| 33 | Under the new tax regime most section 10(14) exemptions were withdrawn, and the engine does not model that | Data/Pay | `data/pay/allowances.json` carries one `taxable` boolean per allowance, and `src/lib/pay/engine.ts` reads it the way the brief specified — an allowance marked `taxable: false` is left out of taxable salary under BOTH regimes. That is right for the old regime. Under section 115BAC the exemptions that survive are narrower: the tax dataset's own `exemptions` list already records it, with `10-14-hill-remote`, `10-14-cea`, `10-14-running-allowance`, `10-13a-hra` and `10-5-ltc` all carrying `regimes: ["old"]` and only `10-14-uniform`, `10-14-transport-disabled` and `10-14-tour-daily` carrying `new`. So the new-regime figure OVERSTATES the exemption for a reader drawing Risk and Hardship, Tough Location, Tribal Area, Hard Area, Island Special Duty or Special Duty (North East) Allowance | Map each `taxable: false` allowance to the exemption provision it relies on — a field on the allowance record, not a table in the engine — and make the engine drop an allowance from taxable salary only under the regimes that provision lists. It needs a schema change on both sides (`schemas/pay-allowances.schema.json` and `src/modules/pay/schema.ts`) and one human decision per allowance about which limb of 10(14) it sits under |
| 34 | Two statutory limits live in prose, so the engine hard-codes them | Data/Pay | The ₹2,37,500 ceiling on basic pay plus Non-Practising Allowance and the two-child limit on Children Education Allowance are both sentences in `conditions[]` on their allowance records, with no field to read them from. `src/lib/pay/engine.ts` carries them as `NPA_PAY_CEILING` and `TWO_CHILD_LIMIT`, each with a comment naming the condition it came from, and each is asserted by a golden test | Add a `limits` object to the allowance schema — `{ key, measure, value }` in the shape `rates[]` already uses — and move both figures into the data, so the calculator and the condition text cannot drift. Worth doing when the next allowance turns out to carry a limit in prose; two is not yet a pattern |
| 35 | Every step from a private cost to company to a take-home is an assumption, and none of them is data | Data/Pay | There is no dataset behind the private side of the comparison, and there cannot be: a CTC is one number that different employers break up differently. `src/lib/pay/compare.ts` exposes the share of the package that is basic pay (40%), the share of basic that is House Rent Allowance (40%), whether the package includes the employer's Provident Fund and gratuity (yes), Provident Fund at 12% each side and gratuity accrual at 4.81% — every one an editable input, with all of them printed under the answer rather than hidden | Nothing to fetch. The row exists so that the private figure is never read as being on the same footing as the government one, which is sourced line by line. If this is ever presented outside personal use, the panel should carry the same disclaimer treatment `verify: true` gets |

| 36 | The Hindi issue of CSMOP 2022 has a text layer that cannot be read | Data/Drafting | `https://www.darpg.gov.in/static/uploads/2025/10/8b5d6eb6c7c47bc69e271e25f1c2cc43.pdf` (284 pages) was fetched and extracted with PyMuPDF, which works perfectly on the English issue. The fonts are Mangal — real Unicode Devanagari — but the glyph map is a legacy one, so every page comes out scrambled: `अभधकायी` where the page renders `अधिकारी`, `ऩयभ अग्रता` where it renders `परम अग्रता`. The scrambling is a glyph substitution with genuine ambiguity (the i-matra and `म` map to the same output glyph), so it cannot be reversed by a table. The pages themselves are correct, so the Appendix 8.1 specimens (Hindi printed 121-130), 8.4 (108-110) and 6.13 (64) were rendered at 130 dpi and read by eye — the route ADR-016 records for the scanned pay orders. `tesseract` is not installed on this machine, so no OCR pass was possible | Nothing to fetch: DARPG's published file is the file. If more of the Hindi issue is needed — Chapter 7's noting conventions, Appendix 8.2's Gazette instructions — render and read those pages the same way, and record which pages were read. Installing `tesseract` with `hin` would let a script check a rendering against what was typed |
| 37 | Four of the ten CSMOP forms have no template yet | Data/Drafting | `data/drafting/templates/` covers the fourteen types this session's brief named. CSMOP 8.4 lists ten forms, and four of them are not among the fourteen: Office Order 8.4(4), Order 8.4(5), Resolution 8.4(7) and Press Communiqué/Press Note 8.4(8). Their specimens have been read and are summarised in `docs/CSMOP-FORMATS.md` §2.4 and §2.8, and the Hindi for all four is already in `data/drafting/structure-terms.json` | Add four more `DocTemplate` files to `scripts/ingest/drafting_seed.py`. Each is the same chassis the O.M. already uses with a different title and a different closing, so the work is content rather than engine. The Office Order is the more useful of the four — it is what a sanctioned leave application comes back as |
| 38 | Seven of the fourteen templates are forms CSMOP prescribes no format for | Data/Drafting | Circular, leave application, representation, RTI reply, show-cause reply, tour programme and T.A. bill covering letter are documents the Central Secretariat produces daily; the manual does not give a format for any of them. Each carries `verify: true`, a `csmopRef.chassis` naming the form whose format it borrows, and a bilingual `csmopRef.note` saying what CSMOP does and does not contribute. `tests/drafting-data.test.ts` fails a template that claims to be unverified without both | The chassis is what the manual supports and that will not change. What would settle the rest is the governing instrument for each: the CCS (Leave) Rules, 1972 for the leave application, the RTI Act 2005 §7(8) for the appellate-authority line, the CCS (CCA) Rules, 1965 for the show-cause reply, and the Travelling Allowance Rules for the tour programme and the bill. Fetch each and cite it per record, rather than leaving the whole template on the manual's chassis alone |
| 39 | Twenty-eight structural terms are the glossary's Hindi, not the manual's | Data/Drafting | `data/drafting/structure-terms.json` holds 78 terms. Fifty are what the Hindi CSMOP prints, read off the rendered page, and carry a `csmopRef`. The rest — enclosure, annexure, circular, minutes, PUC, fresh receipt, flag, self-contained note, running summary, level of disposal, channel of submission, and the designations the manual never signs a specimen with — are not in the manual at all. They carry `verify: true` and cite the Saral Prashasanik Shabdavali (`https://rajbhasha.gov.in/sites/default/files/saralshabdavali.pdf`, 651 pages, fetched). That PDF's own Hindi text layer is partly mis-mapped too (`समिनत` for `समिति`), so the terms taken from it were read off rendered pages as well | Check them against the Department of Official Language's own _Prashasanik Shabdavali_ term list rather than the simplified-usage volume, and against the Central Translation Bureau's chapter on administrative Hindi (`ctb.rajbhasha.gov.in/pdf/chapter6.pdf`). Until then the UI owes a `verify: true` term the same marigold banner the Law Converter shows for curated Hindi |
| 40 | The `.docx` has not been opened by a word processor | Tooling/Drafting | The session's acceptance check is `soffice --headless --convert-to pdf`. LibreOffice is not installed on this machine and is not in CI — `which soffice`, `which libreoffice` and `/Applications/LibreOffice.app` all miss. What IS checked, in `src/lib/drafting/docx.test.ts`, is everything a word processor does before it renders: the archive is unzipped with Node's own zlib and asserted to carry the three parts ECMA-376 requires (`[Content_Types].xml`, `_rels/.rels`, `word/document.xml`), `word/document.xml` is parsed with `DOMParser` and asserted well-formed against a subject line containing every character XML must escape, the page is A4 with 1-inch margins, and all fourteen templates export in both languages with no unresolved placeholder. `tests/e2e/draft.spec.ts` additionally reads the file that really landed on disk from a real browser download | Add a `soffice --headless --convert-to pdf` step to `.github/workflows/ci.yml` behind `if: hashFiles(...)`-style availability, or install LibreOffice in the CI image. Until then the un-checked risk is a rendering fault rather than a structural one — a font substitution or a margin a word processor disagrees about |
| 41 | Five tests fail under CPU contention and pass in isolation | Tooling | Two sessions running their suites at once on this machine reproduced it independently. The unit ones are `src/app/App.a11y.test.tsx` (axe over the lazily-loaded `/pay`, `/draft`, `/learn` routes) and `src/modules/law/converter-page.test.tsx` ("says so when nothing matches" — a search over 1,059 sections that took 5.4s against its timeout). The e2e one is `tests/e2e/law.spec.ts` "opening a section keeps the search box on a desktop", whose `window.scrollY < 40` assertion read 68 under load. All five pass alone, and the whole e2e suite passes at `--workers=1`; the Session 8 CSS was checked and is scoped entirely to `.a4-page` / `.a4-print-root` and `@media print`, so it cannot reach the law page | Give the two timing-sensitive unit files more headroom (a longer `testTimeout`, or `await` the lazy route's own ready signal rather than a fixed wait), and make the law scroll assertion wait on the card's own position rather than a bare `scrollY` threshold. Neither is a product defect; both are assertions that measure the machine as much as the app |
| 44 | India Code's new host `indiacode.gov.in` returns 502 and its handle pages are down | Rules | `https://indiacode.gov.in/robots.txt` and `/` both answer 502 Bad Gateway from nginx. The legacy host `www.indiacode.nic.in` serves a migration notice at `/` and errors on `/handle/...`, but `/bitstream/...` still works and served a clean PoSH text layer with all thirty sections — better than the MWCD gazette scan, whose OCR reads `(1)` as `(/)` and `Committee` as `Committec` | Re-check both hosts before the next authoring session. Any India Code fetch stays hand-run, one document at a time, rate-limited to one request every three seconds, and never on a cron (ADR-012) |
| 45 | The Hindi FR/SR compilation has no text layer at all | Rules | `Compilation_FR_SR_Hindi.pdf` is 20 MB over 296 pages and `pymupdf` extracts **zero** characters — it is a scan, not a typeset document. The English compilation extracts cleanly and is what `data/rules/text/fr-sr.json` is built from | If FR/SR Hindi is ever needed beyond authored headings, it is an OCR job on a 296-page scan, and the result would need reading by eye the way the pay orders were (ADR-016) |
| 46 | Stage C of the question pipeline is not blind in the way a second reader would be | Rules | The worksheet `qa_pipeline.py blind` prints hides the stored key and shows the rule text, and the answers are a real re-derivation — it caught that stage A had put the correct answer at index 0 in 91% of its questions. But the same session wrote the questions, so it cannot rule out an error shared between writing and answering. The critic pass and the independent duplicate check in `tests/rules-data.test.ts` are what compensate | Run `qa_pipeline.py blind` from a fresh session and overwrite `review/blind-<act>.json` with those answers. A mismatch auto-rejects, so the merge needs no code change |
| 47 | The ISTM PoSH guidebook was not fetched; the MWCD handbook stands in for it | Rules | `istm.gov.in` resolves and answers, but only after the CA-chain fix in `authoring_common.build_ca_bundle()` — it chains to _ISRG Root YR_, newer than our pinned `certifi` — and its pages are slow enough that a plain fetch of its home page timed out at 120 s on the first two attempts. No specific ISTM PoSH guidebook URL was identified before the fetch list was fixed. What was fetched instead is the Ministry of Women and Child Development's own _Handbook on the Sexual Harassment of Women at Workplace Act, 2013_ (`posh-handbook-en.pdf`, 1.8 MB), which is the training text ISTM's material cross-refers to. It is fetched and cited but not split into rule records — the Act itself is | Browse `istm.gov.in`'s reading-material section for the PoSH guidebook, add it as a `Document` on the `posh` act in `authoring_common.ACTS`, and re-run `fetch_sources.py --act posh`. It is context for the authored questions, not a source of rule records, so nothing in `data/rules` changes |
| 48 | Every one of the 1,891 glossary entries is compiled, not read off one fetched page | Data/Utils | `data/glossary.json` was meant to start from the Department of Official Language's own Prashasanik Shabdavali (`rajbhasha.gov.in`), the same 651-page PDF `structure-terms.json` already cites (row 39). `WebFetch` on that PDF hit the tool's 10 MB response ceiling, and the page that lists the term-by-term glossary (`/en/administrative-terminology`) answers "Request Rejected". So this dataset takes the brief's own fallback: seven parallel authoring passes (one per category — designation, office, file, finance, establishment, legal, it) compiled standard, well-established Rajbhasha administrative vocabulary from general knowledge, the way a CSTT _Shabdavali_ or an ISTM training module would print it, rather than reading it off one cited page. Every entry therefore carries `verify: true` unconditionally — there is no `verify: false` tier in this dataset the way `structure-terms.json` has one for its fifty CSMOP-sourced terms — and cites the Shabdavali PDF by name and URL as the general reference rather than a page number | Work through `data/glossary.json` against the Department of Official Language's own term list and the Central Translation Bureau's chapter on administrative Hindi (`ctb.rajbhasha.gov.in/pdf/chapter6.pdf`) — the same next step row 39 names — flipping `verify` to `false` only for an entry actually checked against a fetched page |
| 49 | The GPF interest rate could not be fetched from `dea.gov.in` directly | Data/Utils | `WebFetch` on every Department of Economic Affairs interest-rate page failed certificate verification this session (the same class of failure `authoring_common.build_ca_bundle()` works around for `documents.doptcirculars.nic.in` and `istm.gov.in`, but that fix lives in the Python authoring pipeline, not in `WebFetch`). `data/pension/pension-facts.json`'s `gpf.rateHistory` (7.1% p.a., unchanged since 01.07.2020) is corroborated instead across several independent financial-press reports of the same DEA resolution, and `gpf.verify` is `true` | Re-fetch the DEA rate page once its certificate chain is fixed, or read it the way the pay orders are — a rendered scan checked by eye — and flip `verify` to `false` |
| 50 | A handful of portal URLs in `data/portals.json` are the parent department's page, not a dedicated one | Data/Utils | SPARROW has no single canonical site — it is run per cadre (SPARROW-IAS, SPARROW-ACC, …) — so its record points at `dopt.gov.in` with a note explaining why. e-HRMS's own domain returned no authoritative result in search this session, so `https://ehrms.gov.in` is unconfirmed. CSSS likewise has no dedicated public site and points at `dopt.gov.in`. All three, like every record in this dataset, carry `verify: true` | Confirm e-HRMS's live domain directly (the DoPT O.M. announcing eHRMS 2.0 would name it), and record SPARROW's per-cadre URL pattern if DoPT publishes one centrally |
| 51 | The 2026 DoPT holiday circular was read from a secondary reproduction, not fetched from `dopt.gov.in` | Data/Utils | Every direct fetch of `dopt.gov.in` this session answered either HTTP 403 or failed certificate verification — the linked Annexure-I/Annexure-II PDFs were unreachable by the same two failure modes. `scripts/ingest/holidays.py`'s `YEARS[2026]` table was read off a secondary page that reproduces the O.M.'s dated lists in full (`govtstaff.com`, citing F.No. 12/2/2023-JCA dated 03.07.2025); every day-of-week was independently recomputed from the date, never trusted from that page, and `build()` would raise on the first mismatch — none occurred. `data/holidays/holidays-2026.json` carries `verify: true` accordingly | Re-fetch `dopt.gov.in`'s own Annexure-I/II PDFs once the 403/certificate issue clears, cross-check every date against them, and flip `verify` to `false` |
| 52 | The CCS (Commutation of Pension) Rules, 1981 table was not parsed from the fetched PDF | Data/Utils | `persmin.gov.in/pension/rules_new/ccs_coprules_1981_060613.pdf` was fetched, but `WebFetch` could not extract its Schedule table from the PDF's compressed content stream. The 62-row table in `data/pension/pension-facts.json` (`commutation.table`, ages 20-81) was instead cross-checked against two independent secondary reproductions of the same official table, which agreed on every row compared (ages 55-62, checked byte-for-byte) — the same "best public alternative, `verify: true`, recorded" fallback `glossary.json` takes | Parse the fetched PDF with `pdfplumber`/`PyMuPDF` (the way the OCR'd pay orders are read, ADR-016) to confirm the full table against the primary source, then flip `commutation.verify` to `false` |
| 53 | The Trainer's daily reminder can only fire while a tab of this app is already open | Rules | The master context rules out a backend and a push server, and `src/app/pwa.tsx` registers a workbox-generated service worker with no custom message handler to extend. `src/modules/trainer/reminder.ts#checkAndShowReminder` checks the reader's chosen time on every `useNow(60_000)` tick and calls `registration.showNotification()` once permission is granted — best-effort, and a structural ceiling rather than a bug (ADR-027 #7) | Real background delivery needs either a push server (out of scope, master context: zero paid services) or the Periodic Background Sync API, which is Chrome-only, needs its own permission prompt, and has no fallback for Safari/Firefox — worth revisiting only if readers report the local-tab version is not enough |
| 56 | Onboarding's "enable relevant Trainer acts" preset covers nine organisations, not all twenty-two | Trainer/Onboarding | `src/modules/onboarding/actHints.ts#trainerActHintsForJob` narrows `TrainerSettings.actsEnabled` only for organisations self-evidently about intelligence, investigation or classified material (`ib`, `nia`, `cbi`, `ncb`, `ed`, `capf`, `delhi-police`, `mod-civ`, `drdo`), and shows the RTI Act s.24 Second-Schedule note only for the two named there beyond any reasonable dispute (`ib`, `nia`). Every other post in `data/pay/jobs.json` gets no change — `actsEnabled: []` already means every rule book, so this is deliberately conservative rather than a legal classification this app is not positioned to make for the other thirteen | A human familiar with each organisation's actual RTI Second Schedule / classified-work status reviews the remaining posts and extends the two lists in `actHints.ts`, one organisation at a time — not a bulk guess |
| 57 | ~~The deployed CSP's `connect-src 'self'` will refuse the AI layer's Tier 1 BYOK call~~ **RESOLVED** (Session 24) | AI/Security | `public/_headers` now sends `connect-src 'self' https://api.anthropic.com https://huggingface.co https://*.huggingface.co https://*.hf.co https://raw.githubusercontent.com`, and `script-src 'self' 'wasm-unsafe-eval'` for the WebAssembly runtime Tier 0 compiles. `'unsafe-eval'` is still absent, and `tests/e2e/csp.spec.ts` now compares both directives as TOKEN LISTS rather than with `toContain` — a substring check would have been satisfied by `'wasm-unsafe-eval'` and passed while the policy said the opposite. Tier 2's origin is appended at build time by `vite.config.ts` when `VITE_AI_PROXY_URL` is set, so the policy and the feature ship together. ADR-037 | Nothing. The remaining wildcard question is row 69 |
| 58 | Lighthouse mobile Performance is 85-93, not the 95 the session brief asked for | Perf | Measured 2026-08-29 on all seven routes against `scripts/serve-dist.mjs` (production `_headers`, compression, SPA fallback); reports committed under `docs/lighthouse/`. Accessibility 100, Best Practices 96-100, SEO 100 — those three are met. Performance rose from a starting 72-92 through: ADR-031's per-language i18n chunks, one variable Devanagari face replacing four static weights, the rupee sign resolved to that face instead of Inter's 85 KB `latin-ext` subset, two preloaded faces, per-route preload hints, and a static app shell in `index.html` that took First Contentful Paint from 2.6s to ~1.0s at CLS 0. What remains is one metric: Largest Contentful Paint at 3.2-4.1s where >=95 needs about 2.5s, and it is 89% _render delay_ rather than network — the LCP element is text that does not exist until the entry chunk, the module chunk, the view chunk and that view's data have each been fetched and executed in sequence. Preloading each route's full transitive import graph was tried twice and was worse both times (`/law` 90 -> 87). | LCP cannot be fixed by tuning: a paint can come from HTML, which is why the app shell fixed FCP, but LCP updates to whatever larger element appears later. It needs the route's _content_ in the served HTML. Two real options, each wanting its own ADR: (a) prerender the six routes at build time into per-route HTML files, which conflicts with the single-`index.html` SPA fallback in `public/_redirects` and with data living in lazy `?raw` chunks (ADR-013, ADR-018); (b) reduce the chain depth by folding each module's default view into its module chunk, so `/law` is two hops rather than three - cheaper, smaller payoff, and measurable in an afternoon. Re-measure with `pnpm build && pnpm lighthouse` before either; the committed reports are the baseline. |
| 59 | The service worker's "Ready to work offline." toast blocks a primary action on a phone | PWA/A11y | `src/app/pwa.tsx`'s `PwaNotices` renders at `fixed inset-x-3 bottom-[4.5rem]` below `sm`, which is a full-width bar sitting directly above the tab bar, and it waits for the reader to press its `×` rather than timing out. On a 412px viewport that is exactly where the Rules Trainer mock test's "Next question" button is, and the toast intercepts every click on it: a reader who installs the app and immediately starts a mock test cannot finish one until they notice the dismiss control. Found by the `mobile-chromium` Playwright project added this session — `tests/e2e/learn.spec.ts` now calls `dismissPwaToasts(page)`, which is what a real reader does, and `tests/e2e/fixtures.ts` carries the reason at that helper. The desktop layout is unaffected (`sm:right-4 sm:w-80` makes it a corner card) | Auto-dismiss the purely informational "Ready to work offline." notice after a few seconds and leave the actionable "A new version is ready." one waiting, or move the phone-sized toast above the content rather than over it. Either is a change to `src/app/pwa.tsx`, not to a test |
| 60 | The drafting agent can fail a legitimate run because the officer's own document text names a rule number | AI/Drafting | `runAgent`'s `validateCitations()` rejects any answer stating a section, rule or paragraph number that appears in no CITED context snippet, and it runs over the WHOLE final answer. For `law-explain` that is exactly right. For `draft-assist` the answer carries the officer's document text, so a body paragraph that legitimately says "in continuation of para 3 above" or "Rule 18 of the CCS (Conduct) Rules" is checked the same way, and the run ends with `invalid_citation` unless the number appears in a snippet the rationale cited. Mitigated rather than solved: the officer's own brief is context snippet [1], the template spec (with its `csmopRef` paragraphs) is [2], `prompts/drafting.md` tells the model not to state a provision number the brief or a tool result did not give it, and the failure is closed — the panel shows "That run did not finish" rather than a document with an invented citation in it. Not reproduced against a real model this session; the live smoke test is skipped by default and was not run. The direction of the failure is the safe one — an invented rule number in a signed document is the worst thing this feature could produce (ADR-032 §1) — but a correct draft refused with an opaque message is a real cost. | Either give `runAgent` a per-agent policy for which PART of a structured answer the citation rule applies to (the `rationale` field, not `fieldValues`), which is the principled fix and needs `validateCitations` to take a string rather than reading `run.text`; or add the brief and the current values to `citedNumbers` unconditionally, which is weaker and would let the model launder a number through the brief. Prefer the first. Measure first: run `AI_LIVE=1 ANTHROPIC_API_KEY=… pnpm exec vitest run src/ai/agents/drafting.live.test.ts` with a brief that names a rule, and see whether it actually bites. |
| 61 | ~~Turning the drafting AI on will fail on the DEPLOYED site until `public/_headers` allows `api.anthropic.com`~~ **RESOLVED** (Session 24) | AI/Security | Closed with row 57, of which this was the reachable half. Not verified against the live Cloudflare Pages deployment — `tests/e2e/csp.spec.ts` replays the policy from `public/_headers` onto the document in a real browser on every route, which is the closest this repository can get without a deploy and a key | Whoever next deploys should press "Test connection" in Settings on the live site once, with a real key, and confirm it returns rather than reporting an opaque provider error |

| 62 | Rule-card coverage is 100% only for 5 of 12 rule books, not the 11 the session brief asked for | Rules | `make_cards.py`'s `_unanswerable()` was fixed this session (see ADR-033) to catch a real class of ambiguous rule card it previously missed entirely for FR/SR and CSMOP — a heading-only check that could never fire once a book prints no headings at all. Running that fix over the corpus found MORE genuinely ambiguous or emptied rules than the exact-string check it replaced: ccs-cca (29/34), ccs-leave (58/75), ccs-pension (69/86), gfr (242/307), rti (25/31), osa (13/14) and fr-sr (63/77) each have a small number of rules correctly excluded — deleted/omitted/cancelled provisions with nothing to recall, or a heading/prompt that reads the same as another rule's and cannot be answered from the card alone. ccs-conduct, posh, ol-act, ol-rules and csmop are at literal 100% | This is a data-quality fix, not a shortfall to force closed: making the excluded acts hit literal 100% would mean either reverting to the weaker check (reintroducing the bug) or hand-disambiguating on the order of 125 individual rules across 7 books with a distinguishing phrase per rule. Worth doing incrementally, book by book, starting with the acts closest to full (osa is 1 rule short, ccs-cca 5) rather than as one pass |
| 63 | A full Hindi-quality pass over `src/i18n/hi.json` was started and did not finish | i18n | Session 20 dispatched a review pass over all ~995 keys (grammar, register, transliteration-vs-translation, cross-file terminology consistency against `data/drafting/structure-terms.json` and `scripts/authoring/hindi/*.json`) but it was cut off by a session rate limit before reporting any findings or making any edits — `src/i18n/hi.json` is unchanged from before this session as a result | Re-run the same pass in a fresh session: read `hi.json` and `en.json` side by side in full, fix confident errors directly, leave debatable word choices noted rather than changed, and validate with `pnpm i18n:check` afterward |
| 64 | `data/glossary.json`'s 1,891 entries and the rest of `data/` were not re-reviewed this session | i18n | Row 48 already tracks the glossary's own gap (compiled from general knowledge, not read off one fetched page, `verify: true` unconditionally). This session's Hindi-authoring effort went into `scripts/authoring/hindi/*.json`, `data/law/overlays/*.json` and the Rules Trainer's own cloze/MCQ content — all reviewed through the four-stage pipeline or a dedicated overlay QA pass — but did not extend to re-reviewing already-shipped datasets (`data/glossary.json`, `data/drafting/*.json`, `data/pay/*.json`, `data/pension/*.json`, `data/holidays/*.json`, `data/portals.json`) for the grammar/register/consistency angle the brief asked for | A dedicated pass, dataset by dataset, checking each `hi` string the same way row 63's `hi.json` pass was meant to — grammar, register, transliteration, and consistency against the structure-terms/rules-hindi vocabulary already established |
| 65 | `pay_orders.py`'s Dearness Allowance watch can usually only ask a human, not answer on its own | Pay/Automation | The Department of Expenditure's own DA order PDFs are mostly scanned images with no usable text layer — two real orders fetched this session (the ones `data/pay/da-history.json` cites for 01.07.2025 and 01.01.2026) extract to zero characters through `pdfplumber`; a third real fetch (`ta-rules-om-2018.txt`, `scripts/ingest/fixtures/pay-orders/`) DOES carry an OCR'd text layer, so it is not universal, just common enough that "Manual review" is the expected outcome of most runs, not an edge case (ADR-034 §1) | Add an OCR fallback (pytesseract, or PyMuPDF's own rendering plus a vision-capable pass) for the case where `pdfplumber` finds nothing, with the same regex applied to the recognised text and the same "propose only on a confident match" rule; measure recognition quality against the two known-empty fixtures before trusting it with a write |
| 66 | The law research agent's citation check has one known false-failure mode, and it is the same one #60 names for drafting | AI | `validateCitations()` rejects a provision number appearing in no CITED snippet, and `runLawAgent` runs it a second time itself over `answer.en + answer.hi` with `requireCitation: true`. Two mitigations are in place and both are tested: the tool results ARE the snippets (so anything read is supportable), and each snippet writes a provision as `Section 318(4) of the BNS` so the sub-section form is extractable rather than being split into `318` and `4` by the bare-numeral fallback. What is left: a caveat the MODEL writes that names BNSS s.531(2) — the saving provision — fails the whole run, because the date rule is deliberately not in any snippet. `src/ai/prompts/law.md` tells the model not to write the date rule for exactly this reason, and `dateRuleCaveat()` adds it in code instead | The principled repair is #60's: a per-agent policy for WHICH PART of a structured answer the rule applies to, not widening what counts as supported. Read #60 first — it is the same decision for both agents. It fails closed, which is the safe direction |
| 67 | ~~Tier 0 (WebLLM) has a law-agent policy and no provider to run it~~ **RESOLVED** (Session 24) | AI | `LocalProvider` is real (ADR-037) and `policyForTier('local')` is now reachable: the research pass gets one tool-calling turn, one tool result and a two-sentence answer, which is also what `LOCAL_MAX_TOOL_STEPS` independently enforces at the provider. `src/ai/providers/local.test.ts` runs the whole loop — emulated call, real tool, cited answer — through `runAgent` unchanged against a scripted generator. Still not exercised against a real downloaded model end to end; that is row 13 | Nothing here. Row 13 carries what is left |
| 68 | The on-device model picker quotes graphics memory, not download size, because the package states no download size | AI | `LOCAL_MODELS[].vramMB` is `vram_required_MB` read verbatim out of `@mlc-ai/web-llm`'s own `prebuiltAppConfig` — the only size figure the library publishes, and `src/ai/local/catalogue.test.ts` asserts each of the five matches it to the hundredth of a megabyte. It is an upper bound on the download rather than the download: the difference is the key-value cache, which is allocated on the GPU rather than fetched. So "1.6 GB" on a metered connection is honest and slightly pessimistic, and `ai.local.sizeNote` says which of the two it is in both languages. Deriving the real figure would mean a HEAD request per shard against Hugging Face at build time, which would put a network call into the build and pin a number that changes when mlc-ai re-quantises a model | Read the real total once, by hand, from each model's `ndarray-cache.json` on Hugging Face, and store it beside `vramMB` as a second `verify: true` field — the same treatment `data/pay/allowances.json` gives a figure nobody fetched. Do not compute it in the build |
| 69 | Tier 0's `connect-src` names two wildcard subdomains, which is wider than this project would like | AI/Security | Hugging Face serves model weights through an LFS CDN whose hostname is neither fixed nor published — a `huggingface.co/.../resolve/main/...` request redirects to some `cdn-lfs-*.hf.co` or `cas-bridge.*.hf.co` host that varies by region and by day. `public/_headers` therefore permits `https://*.hf.co` and `https://*.huggingface.co` alongside the two exact origins. Enumerating them was attempted and abandoned: a list that is wrong on the day it is wrong produces a download that stalls with a `securitypolicyviolation` and no error a reader could act on, which is worse than a wide directive behind a consent gate. Narrowed as far as it goes in three other ways — the hosts are refused entirely unless the reader picks Tier 0 and presses Download, `src/ai/local/engine.ts` narrows the engine's `model_list` to the five models this app offers so no other record's URL can be reached, and `tests/no-external-urls.test.ts` confines every one of those strings to the `web-llm` chunk | Either self-host the five models' shards on the app's own origin, which removes the directive entirely and costs about 8 GB of Pages storage and a re-upload per model bump; or watch whether Hugging Face ever publishes a stable CDN hostname and pin it. Neither is urgent: nothing the reader types is on these requests |
| 70 | Eleven of the fifteen Library works have a FLAT table of contents, because no chapter division for them exists in this repository | Library/Data | `data/law/*.json` records a `chapter` per section, so the three Sanhitas get a real nested table of contents with the publisher's own chapter titles. CSMOP numbers its paragraphs `<chapter>.<para>`, so `library_seed.py` groups on the part before the dot — the structure the manual itself prints — but the chapter TITLES are in no dataset here, so each node is labelled by its number alone (`अध्याय 4`) rather than by a heading nobody published. The other eleven rule books (CCS Conduct/CCA/Leave/Pension, FR/SR, GFR, RTI, OSA, PoSH, OL Act, OL Rules) publish chapters or parts in their PDFs that `extract_rules.py` does not capture, so their tables of contents are one flat node per rule — 307 rows for the GFR. The session brief asked for exactly this rather than inventing structure; `tocSource` records which of the three each work got, and the work page states it to the reader in both languages | Teach `extract_rules.py` to capture the chapter heading lines it currently walks past — it already scores a sequence walk over substantive members, and a chapter heading is a line it discards. That writes `chapter` onto each rule in `data/rules/text/*.json`, which the Trainer's Browse screen would also use, and `library_seed.py` picks it up with no change. Do it act by act with the PDF open: GFR's chapters and FR/SR's parts are not numbered the same way, and a parse that "succeeded" on the wrong table is how ADR-023's five refinements were each found |
| 71 | 221 Library units have no ENGLISH heading, 2 have neither, and 71 of the 87 law chapter titles have no Hindi | Library/Data | Three separate one-sided gaps, all pre-existing and all surfaced by building a reading surface over the corpus. (a) Four rule books print no heading `extract_rules.py` could read — the whole of FR/SR (77) and CSMOP (~125), 13 GFR rules, 3 CCS (Pension), 2 CCS (Leave), 1 OL Rules. Session 20 authored HINDI headings for all of them (`scripts/authoring/hindi/*.json`), so those units read better in Hindi than in English, which is the equal-footing rule failing in the direction nobody expects. `library_seed.py` quotes the unit's opening ~96 characters as an `excerpt` so the English table of contents is navigable — the same fallback `make_cards.py` already uses on a rule card front — and `unitLabel()` (`src/lib/library/label.ts`) is the one place that decides, reporting which language it actually returned so the markup can mark it. (b) CCS (Leave) rules 39 and 39D have no heading in either language: their PDF prints the heading inside the first line of text. (c) NCRB Sankalan publishes English chapter titles only, so a Hindi reader sees an English chapter name on all three Sanhitas | (a) is ~221 English headings, authored the way Session 20 authored the Hindi ones and stored beside them in a new `scripts/authoring/english/<act>.json` that `extract_rules.py` folds in — which fixes the Trainer's cards and Browse at the same time, since `make_cards.py` currently puts a truncated first sentence on 110 FR/SR rule-card fronts. (b) is two headings, read off the PDF. (c) is 87 chapter titles, authored, carrying `verify: true` like every other authored Hindi in this project. None is urgent; all three are visible to the reader as what they are rather than papered over |

## Sources

What has been fetched, and what has not. Recorded so a later session does not rediscover it.

| Source                                                                    | URL                                                                             | Note                                                                                                                                                                                                                                                                           |
| ------------------------------------------------------------------------- | ------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **NCRB Sankalan — section tables** ✅ ingested                            | https://www.ncrb.gov.in/uploads/SankalanPortal/SectionTableBNS.html             | Rows are inline HTML in `#example`/`#exampleold`; `ScheduleBNSS.html` is the First Schedule and `ChaptersBNSS.html` §359 the compounding tables. English only                                                                                                                  |
| India Code ❌ unreachable                                                 | https://indiacode.gov.in                                                        | Client-rendered SPA; legacy DSpace host retired. Crawling forbidden regardless — see row 17 and ADR-012                                                                                                                                                                        |
| **DoE — 7th CPC report** ✅ ingested                                      | https://doe.gov.in/files/cenetral-pay_document/7cpc_report_eng.pdf              | 899 pages with a full text layer — the only pay source fetched here that needed no OCR. Source for the Risk and Hardship Matrix, Dress Allowance, Children Education Allowance, Detachment, Deputation, Running Allowance, Night Duty, NPA and the Special Security Allowances |
| **DoE — allowance and DA orders** ✅ ingested (OCR)                       | https://doe.gov.in/files/circulars_document/                                    | Scanned images with no text layer. Read by rendering each page with PyMuPDF and running the macOS Vision text recogniser over it; the figures were then checked against the rendered page by eye. Eight allowance orders plus the two current DA orders                        |
| **CCS (Revised Pay) Rules, 2016** ✅ ingested                             | https://thc.nic.in/Central%20Governmental%20Rules/                              | A 20-page image scan; `doe.gov.in` hosts no text-layer copy. Pages 12-15 are the pay matrix and were read visually. See `docs/DATA-GAPS.md` #24 and `scripts/ingest/README.md`                                                                                                 |
| DoPT / DoPPW orders                                                       | various                                                                         | PDF; needs `pdfplumber` / `PyMuPDF` in the CI pipeline                                                                                                                                                                                                                         |
| ssc.gov.in ❌ unreachable                                                 | https://ssc.gov.in                                                              | Client-rendered Angular application, like India Code. `/notice-board` and every API path tried return the same 80 KB shell — see row 27                                                                                                                                        |
| cghs.gov.in ❌ unreachable                                                | https://cghs.gov.in                                                             | No response within 40 seconds, twice — see row 28                                                                                                                                                                                                                              |
| incometax.gov.in ❌ unreachable                                           | https://www.incometax.gov.in                                                    | 301 with no body; `incometaxindia.gov.in` returns 302 — see row 29                                                                                                                                                                                                             |
| **PFRDA — Unified Pension Scheme** ✅ ingested                            | https://www.pfrda.org.in/schemes/national-pension-system/unified-pension-scheme | The regulator's own scheme page, read directly. It carries the rates and the payout rules but not the notification number — see row 30                                                                                                                                         |
| DoPT annual holiday O.M. ❌ unreachable directly                          | https://dopt.gov.in                                                             | 403 or a broken certificate chain on every attempt this session — see row 51. The 2026 calendar is read from a secondary reproduction of the same O.M. instead, cross-checked as row 51 describes                                                                              |
| Dept of Official Language — Prashasanik Shabdavali                        | various                                                                         | Source for the Hindi administrative glossary                                                                                                                                                                                                                                   |
| **CCS (Commutation of Pension) Rules, 1981** ✅ fetched, table not parsed | https://persmin.gov.in/pension/rules_new/ccs_coprules_1981_060613.pdf           | The rules text was fetched; the Schedule's commutation-value table could not be extracted from the PDF's compressed stream — see row 52                                                                                                                                        |
| DEA — GPF interest rate ❌ certificate failure                            | https://dea.gov.in/budget-division/                                             | See row 49                                                                                                                                                                                                                                                                     |
| data.gov.in                                                               | https://data.gov.in                                                             | GODL-India licence — attribution is mandatory                                                                                                                                                                                                                                  |

<!-- law-ingest:begin -->

### Law dataset coverage - generated by `scripts/ingest/ncrb_sankalan.py`

Counted on 2026-08-29 from the run that produced `data/law/*.json`. Do not edit by
hand: the ingest script rewrites everything between the two markers, and it formats this table the
way Prettier would so `pnpm format` and the weekly cron do not fight over it.

| Code          | Sections | Missing Hindi heading | Missing Hindi text | Missing English text | No classification | No Hindi punishment | No counterpart in the old Act |
| ------------- | -------: | --------------------: | -----------------: | -------------------: | ----------------: | ------------------: | ----------------------------: |
| BNS (`bns`)   |      358 |                     0 |                358 |                    0 |                70 |                   0 |                             0 |
| BNSS (`bnss`) |      531 |                     0 |                531 |                    2 |               n/a |                 n/a |                             0 |
| BSA (`bsa`)   |      170 |                     0 |                170 |                    0 |               n/a |                 n/a |                             0 |
| **All three** | **1059** |                 **0** |           **1059** |                **2** |            **70** |               **0** |                         **0** |

The classification columns count only the BNS: cognizable, bailable and compoundable are properties
of offences, and the BNSS and the BSA are procedural.

Repealed-Act sections with no counterpart at all (NCRB marks them "Deleted"): **42** -
CrPC 2(f), CrPC 2(k), CrPC 2(q), CrPC 2(t), CrPC 8, CrPC 10, CrPC 16, CrPC 17, CrPC 18, CrPC 19, CrPC 27, CrPC 144A, CrPC 153, CrPC 355, CrPC 404, IEA 22A, IEA 65B(3)(a), IEA 65B(3)(b), IEA 65B(3)(c), IEA 65B(3)(d), IEA 82, IEA 88, IEA 113, IEA 166, ....

Parse path taken this run (see `scripts/ingest/README.md`): BNS `inline-html`, BNSS `inline-html`, BSA `inline-html`. `inline-html` means the
correspondence table's rows were in the HTML, as they have been on every run so far; `pdf` means they
were not, and `pdfplumber` read the bare-act PDF instead.

The full per-section lists are in `scripts/ingest/reports/law-gaps.json`, which is committed so a
reviewer can diff coverage between runs without re-running the ingest.

<!-- law-ingest:end -->
