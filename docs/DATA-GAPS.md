# Data gaps and open items

Anything unverified, unreachable, or deferred. Every row must say what was tried and what to do next.
Sessions add rows; sessions that resolve one delete it and note the resolution in `docs/DECISIONS.md`.

## Open

| #   | Item                                                                | Area       | What was tried                                                                                                                                                                                                                                                                                            | Next step                                                                                                                                                              |
| --- | ------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `LICENSE` carries the literal placeholder `<YOUR NAME>`             | Legal      | Kept the placeholder at the human's explicit instruction; `git config user.name` reads "Ayush Singh"                                                                                                                                                                                                      | Human replaces `<YOUR NAME>` with the copyright holder before the repo is made public                                                                                  |
| 2   | Colour contrast is not verified in a real browser                   | A11y       | axe cannot evaluate `color-contrast` under jsdom — no layout or paint, so the rule reports _incomplete_, not _pass_, and is disabled in `App.a11y.test.tsx`. Covered instead by `src/styles/tokens.test.ts`, which does the WCAG maths on the shipped token values in both themes                         | Run axe through Playwright against the built app in both themes and both languages                                                                                     |
| 3   | Devanagari glyph rendering is not verified in a real browser        | Typography | `PageHeader.test.tsx` verifies the token chain (`tokens.css` → Tailwind → the `<h1>`) and that Tiro Devanagari Hindi is first in the stack. jsdom cannot load fonts or rasterise                                                                                                                          | Playwright: assert `document.fonts.check()` for Tiro, and screenshot a Hindi heading                                                                                   |
| 4   | "No user-entered value leaves the page" is not yet checked at runtime | Privacy   | `tests/no-external-urls.test.ts` proves it statically (every fetchable reference in built HTML/CSS/JS is same-origin, swept against an allowlist of inert vendor strings). `tests/e2e/zero-third-party-requests.spec.ts` now proves the runtime half of "zero cross-origin requests" — every route plus a language toggle, watching real `page.on('request')` traffic | No form captures user input yet, so there is nothing to assert *that* against. Extend the same test once Drafting Studio or the Pay calculator has real fields |
| 6   | No dataset has been ingested                                        | Data       | `data/_meta/versions.json` holds a single placeholder entry for the shell                                                                                                                                                                                                                                 | Sessions 2+: NCRB Sankalan section tables, then `/schemas` + `jsonschema` validation in CI                                                                             |
| 7   | An explicit theme opposite to the system setting still flashes once | A11y/UX    | ADR-006 removes the flash whenever the system preference matches the reader's choice, in CSS alone. Someone who explicitly picked light on a dark system (or the reverse) still sees one frame of the other palette, because IndexedDB cannot be read before first paint                                  | Accept, or revisit only if readers report it — a synchronous pre-paint read would mean storing state outside IndexedDB                                                 |
| 8   | `@lhci/cli` can no longer produce a Lighthouse "PWA" category score | Tooling    | `pnpm dlx @lhci/cli` resolves `lighthouse@12.6.1`; Lighthouse removed the PWA category and its audits (`installable-manifest`, `service-worker`, `maskable-icon`, …) upstream around v10. Verified installability instead with a one-off `pnpm dlx lighthouse@9` run against `pnpm preview` — see ADR-008 | If a current-Lighthouse PWA signal is needed again, re-run the `lighthouse@9` one-off, or switch to Chrome DevTools Protocol's `Page.getInstallabilityErrors` directly |
| 9   | `OfflineBadge` can't tell a captive portal / dead uplink from a real connection | PWA | It reads `navigator.onLine`, which is true whenever there's a network interface — not whether the internet is actually reachable. Now that a service worker exists, a better signal is available | Have the badge (or the SW) probe a small same-origin resource periodically or on `online`, rather than trusting `navigator.onLine` alone |
| 10  | Manifest `lang: 'hi'` sits in tension with "equal bilingual footing" | Product   | This session's brief specified `lang: 'hi'` literally for the web manifest (`vite.config.ts`); the actual in-page language is detected per reader and is often English (`detectBrowserLanguage`) | Human product decision: keep `hi`, switch to `en`, or accept the mismatch as a manifest-spec limitation (no per-reader manifest is practical) — see ADR-008 addendum |

## Sources not yet attempted

Recorded so a later session does not rediscover them. Reachability was confirmed this session only for NCRB
(HTTP 200); nothing has been fetched or parsed.

| Source                                             | URL                                                                 | Note                                                                                               |
| -------------------------------------------------- | ------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| NCRB Sankalan — BNS section table                  | https://www.ncrb.gov.in/uploads/SankalanPortal/SectionTableBNS.html | Reachable (200). Static HTML tables; BNSS and BSA equivalents alongside, PDFs under `DownloadPDF/` |
| India Code                                         | https://indiacode.gov.in                                            | `robots.txt` disallows bots — fetch manually one-off, cite, never cron                             |
| DoE / DoPT / DoPPW / PFRDA orders                  | various                                                             | PDF; needs `pdfplumber` / `PyMuPDF` in the CI pipeline                                             |
| DoPT annual holiday O.M.                           | various                                                             | Published yearly; the calendar needs a refresh each December                                       |
| Dept of Official Language — Prashasanik Shabdavali | various                                                             | Source for the Hindi administrative glossary                                                       |
| data.gov.in                                        | https://data.gov.in                                                 | GODL-India licence — attribution is mandatory                                                      |
