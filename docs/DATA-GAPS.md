# Data gaps and open items

Anything unverified, unreachable, or deferred. Every row must say what was tried and what to do next.
Sessions add rows; sessions that resolve one delete it and note the resolution in `docs/DECISIONS.md`.

## Open

| #   | Item                                                         | Area       | What was tried                                                                                                                                                                                                                                                                    | Next step                                                                                                   |
| --- | ------------------------------------------------------------ | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| 1   | `LICENSE` carries the literal placeholder `<YOUR NAME>`      | Legal      | Kept the placeholder at the human's explicit instruction; `git config user.name` reads "Ayush Singh"                                                                                                                                                                              | Human replaces `<YOUR NAME>` with the copyright holder before the repo is made public                       |
| 2   | Colour contrast is not verified in a real browser            | A11y       | axe cannot evaluate `color-contrast` under jsdom — no layout or paint, so the rule reports _incomplete_, not _pass_, and is disabled in `App.a11y.test.tsx`. Covered instead by `src/styles/tokens.test.ts`, which does the WCAG maths on the shipped token values in both themes | Run axe through Playwright against the built app in both themes and both languages                          |
| 3   | Devanagari glyph rendering is not verified in a real browser | Typography | `PageHeader.test.tsx` verifies the token chain (`tokens.css` → Tailwind → the `<h1>`) and that Tiro Devanagari Hindi is first in the stack. jsdom cannot load fonts or rasterise                                                                                                  | Playwright: assert `document.fonts.check()` for Tiro, and screenshot a Hindi heading                        |
| 4   | "Zero network requests" is proven statically, not at runtime | Privacy    | `tests/no-external-urls.test.ts` asserts every fetchable reference in built HTML/CSS is same-origin, and sweeps all build output including JavaScript against an allowlist of inert vendor strings (XML namespaces, React/Dexie error-message links, OFL attribution)             | Playwright test that fails the run on any cross-origin request, per the master context                      |
| 5   | No offline capability yet                                    | PWA        | `OfflineBadge` reads `navigator.onLine` only. `vite-plugin-pwa` is in the stack but was not part of Session 1's scope                                                                                                                                                             | Add `vite-plugin-pwa` + Workbox; precache the shell, fonts and `/data`; make the badge service-worker aware |
| 6   | No dataset has been ingested                                 | Data       | `data/_meta/versions.json` holds a single placeholder entry for the shell                                                                                                                                                                                                         | Sessions 2+: NCRB Sankalan section tables, then `/schemas` + `jsonschema` validation in CI                  |

## Sources not yet attempted

Recorded so a later session does not rediscover them. Reachability was confirmed this session only for NCRB
(HTTP 200); nothing has been fetched or parsed.

| Source                                             | URL                                                                 | Note                                                                                               |
| -------------------------------------------------- | ------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| NCRB Sankalan — BNS section table                  | https://www.ncrb.gov.in/uploads/SankalanPortal/SectionTableBNS.html | Reachable (200). Static HTML tables; BNSS and BSA equivalents alongside, PDFs under `DownloadPDF/` |
| India Code                                         | https://indiacode.gov.in                                            | `robots.txt` disallows bots — fetch manually one-off, cite, never cron                             |
| DoE / DoPT / DoPPW / PFRDA orders                  | various                                                             | PDF; needs `pdfplumber` / `PyMuPDF` in the CI pipeline                                             |
| DoPT annual holiday O.M.                           | various                                                             | Published yearly; the calendar needs a refresh each December                                       |
| Dept of Official Language — Prashasanik Shabdavali | various                                                             | Source for the Hindi administrative glossary                                                       |
| data.gov.in                                        | https://data.gov.in                                                 | GODL-India licence — attribution is mandatory                                                      |
