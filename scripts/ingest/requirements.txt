# Pinned so a cron run three months from now parses the same way this one did.
# Regenerate with: pip install -r requirements.in && pip freeze  (there is no .in;
# these are the direct dependencies and pip resolves the rest).
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0
pdfplumber==0.11.4
pymupdf==1.24.14
jsonschema==4.23.0
rapidfuzz==3.10.1
